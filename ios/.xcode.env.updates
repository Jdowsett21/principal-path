# Embed a debug bundle for physical devices so the app can still boot
# when Metro discovery fails and fall back to `main.jsbundle`.
if [[ "$CONFIGURATION" = *Debug* && "$PLATFORM_NAME" != *simulator* ]]; then
  unset SKIP_BUNDLING
fi
